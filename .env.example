# Backend origin only (no path, no trailing slash).
# Must be set at BUILD time so WebSocket URL is correct in the client bundle.
#
# Local:
NEXT_PUBLIC_API_URL=http://localhost:8000
#
# Production (use .env.production or your host's env, then rebuild):
# NEXT_PUBLIC_API_URL=https://at-backend-production-8139.up.railway.app

# Optional: if your backend uses /chats without /api, set to empty:
# NEXT_PUBLIC_API_PREFIX=
