# Backend origin only (no path). Requests will go to {API_URL}/api/chats etc.
# Local:
# NEXT_PUBLIC_API_URL=http://localhost:8000
# Production (Railway) — use the backend URL directly, not via your frontend domain:
NEXT_PUBLIC_API_URL=https://at-backend-production-8139.up.railway.app

# Optional: if your backend uses /chats without /api, set to empty:
# NEXT_PUBLIC_API_PREFIX=
