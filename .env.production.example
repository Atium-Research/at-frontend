# Production: set at BUILD time so the client bundle gets the correct URL.
# Next.js inlines NEXT_PUBLIC_* at build — runtime env is not enough.
#
# No trailing slash.
NEXT_PUBLIC_API_URL=https://at-backend-production-8139.up.railway.app
